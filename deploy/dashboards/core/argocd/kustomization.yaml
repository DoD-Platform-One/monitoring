#apiVersion: kustomize.config.k8s.io/v1alpha1
#kind: Component

namespace: monitoring

commonLabels:
  release: monitoring
  grafana_dashboard: "true"

configMapGenerator:
  - name: argocd
    files:
      - argocd-dashboard.json=dashboard.json