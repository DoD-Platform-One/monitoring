#apiVersion: kustomize.config.k8s.io/v1alpha1
#kind: Component

namespace: monitoring

commonLabels:
  release: monitoring
  grafana_dashboard: "true"

configMapGenerator:
  - name: main
    files:
      - dashboard.json