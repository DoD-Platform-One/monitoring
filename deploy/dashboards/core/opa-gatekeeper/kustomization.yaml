#apiVersion: kustomize.config.k8s.io/v1alpha1
#kind: Component

namespace: gatekeeper-system

commonLabels:
  release: monitoring
  grafana_dashboard: "true"

configMapGenerator:
  - name: opa-gatekeeper
    files:
      - dashboard.json