flux:
  enabled: true

istio:
  enabled: true

global:
  imagePullSecrets:
  - name: private-registry-mil

grafana:
  image:
    pullSecrets:
    - private-registry-mil

kube-state-metrics:
  imagePullSecrets:
  - name: private-registry-mil

prometheus-node-exporter:
  serviceAccount:
    imagePullSecrets:
    - name: private-registry-mil
